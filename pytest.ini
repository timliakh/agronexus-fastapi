[pytest]
testpaths = tests
pythonpath = .
markers =
    smoke: critical API paths (fast subset for CI smoke stage)
    regression: full API regression suite
    admin: admin API endpoints
addopts = --strict-markers
